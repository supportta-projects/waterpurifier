import type { ReactNode } from "react";

type StaffGroupLayoutProps = {
  children: ReactNode;
};

export default function StaffGroupLayout({ children }: StaffGroupLayoutProps) {
  return children;
}

